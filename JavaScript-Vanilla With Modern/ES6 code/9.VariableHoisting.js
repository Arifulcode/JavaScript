// variable hoisting

name = 'Ariful Islam'// before value assign
console.log(name);
var name; //after declare
// (before value assign and then var/value declare )this complete system is called variable Hoisting. 